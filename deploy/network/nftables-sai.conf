# /etc/nftables.d/sai.conf  (incluir desde /etc/nftables.conf)
# Limita el acceso al puente (8080) a la subred local. Ajusta el rango.
table inet sai {
    chain input {
        type filter hook input priority 0; policy accept;
        tcp dport 8080 ip saddr != 192.168.178.0/24 drop
        tcp dport 3493 ip saddr != 127.0.0.1 drop
    }
}
